abstract class Animal { 
    /* Add four (4) Java classes without modifiers named Animal (abstract), Bird, Cat, and Dog.
Animal should be the parent class of the other three (3) classes */
    abstract void eat(); // For the Animal class, declare these three (3) abstract methods: eat(), sleep(), and makeSound()
    abstract void sleep(); // For the Animal class, declare these three (3) abstract methods: eat(), sleep(), and makeSound()
    abstract void makeSound(); // For the Animal class, declare these three (3) abstract methods: eat(), sleep(), and makeSound()
}
